Бэкэнд.

Я не знаю  полностью архитектуры вашего приложения, но у вас есть
/api/{tenantguid}/Priorities
/api/{tenantguid}/Statuses
и
/api/{tenantguid}/Tasks

В /api/{tenantguid}/Tasks есть всё то что в двух первых url.
Ещё я бы прописал поля в /api/{tenantguid}/Tasks на английский
к примеру поля статуса и приоритета присылают значения на русском и
в коде это выглядит не очень
Вот пример:
  <td *ngIf="item.statusName == 'Открыта'"><span class="status status-open">{{item.statusName}}</span></td>
Ещё я не понял, где тут номер заявки.Я нашёл id но не нашёл поле с номером заявки(просто не понял какое из полей),
поэтому я вставил taskTypeId.

Ещё я нашёл ошибки в поле name в url /api/{tenantguid}/Tasks (строка,которая приходит в title заявки, с тегами html)


Фронт

Форма заявки
Я сделал форму, но я не добавляю новые заявки в таблицу(но отправляю запрос на содание и мне приходит id), 
потому что при перезагрузке страницы я не получаю той заявки.

Форма редактирования.
В ответе запроса я не понял где заявитель.Нашёл только исполнителя и создателя
Так же я не понял где мне взять имя и фамилию пользователя, который хочет написать комментари
Форму редактирования я не сделал, потому что она полностью повторяет форму создания, кроме, естественно, url запросов.
Так же в макете есть тайтл "Добавление коментариев" и кнопка.Мне кажется кнопку нужно расположить 
под созданием комментария.

Ещё я использовал EventEmmiterService.Хотел попробовать его при кейсе, когда ваши компоненты изменяются при роутинге, т.е. нет родительского.
Я пришёл к выводу, что этот кейс будет давать какую-либо пользу, если у нас много вложенностей по роутам.
Самым простым вариантом было бы просто запихнуть в другой компонент и оттуда делать emit из дочерних компонентов.Или использовать @ViewChild

Ещё я не написал обработчики ошибок.Обычно я это делаю в продакшене
Просто если пилить приложение(или хотябы такой дашборд) с нуля, то уйдёт очень много времени,
которым я мало обладаю
